# networking
```bash
# create a network (network create <network-name>)
docker create network test
# connect a container to a network (network connect <network> <container>)
docker network connect test jenkins
```

